---
# Note: unity_ads_plugin is added to support Unity Ads integration.
# If the plugin's API differs from the code in GPA/lib/services/unity_ads_manager.dart,
# adjust the import and method calls accordingly.
dependencies:
  unity_ads_plugin: ^2.0.0
---
