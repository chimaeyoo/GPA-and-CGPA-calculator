Note: updated unity_ads_plugin version to ^0.3.30 to match a valid pub.dev release.

If Codemagic still fails during `flutter pub get`, paste the error output here and I will update the dependency version or switch to an alternative Unity Ads plugin.
