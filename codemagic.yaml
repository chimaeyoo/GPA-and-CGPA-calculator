workflows:
  android-workflow:
    name: Build Android
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        working_directory: GPA
        script: |
          flutter build apk --release
    artifacts:
      - GPA/build/app/outputs/apk/release/*.apk

